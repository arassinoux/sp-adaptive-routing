allprojects {
}

subprojects {
    apply plugin: "java"
    version = '1.0'
    repositories {
        mavenCentral()
    }
}

project(':messages') {
}

project(':arms') {
    dependencies {
        compile project(':messages')
    }
}

project(':simulation') {
    dependencies {
        compile project(':messages')
    }
}

project(':agents') {
    dependencies {
        compile project(':messages')
    }
}
